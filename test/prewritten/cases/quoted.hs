fromList
  [ ( "s1",
      fromList
        [ ("single", "with 'single' quotes"),
          ("double", "with \"double\" quotes"),
          ("complex", "security find-generic-password -s \"matterhorn\" -a \"myaccount\" -w")
        ]
    )
  ]
